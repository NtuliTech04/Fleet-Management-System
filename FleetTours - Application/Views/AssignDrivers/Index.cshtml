@model IEnumerable<FleetTours___Application.Models.AssignDriver>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Booking.PayMethod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SelectedDriver)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Booking.PayMethod)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SelectedDriver)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignID })
        </td>
    </tr>
}

</table>
